name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  vet-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go vet ./...
      - run: test -z "$(gofmt -l cmd internal testdata scripts)" || (gofmt -l cmd internal testdata scripts && exit 1)
      - run: go test ./...

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
        working-directory: web
      - run: npm run check
        working-directory: web
      - run: npm run build
        working-directory: web
      - name: bundle budget (warn over 250 KiB uncompressed)
        run: |
          size=$(du -sb web/dist | cut -f1)
          echo "web/dist: $size bytes"
          test "$size" -le 256000 || echo "::warning::web bundle $size bytes exceeds 250 KiB budget"

  cross-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - { goos: windows, goarch: amd64, ext: .exe }
          - { goos: linux,   goarch: amd64, ext: "" }
          - { goos: linux,   goarch: arm64, ext: "" }
          - { goos: darwin,  goarch: amd64, ext: "" }
          - { goos: darwin,  goarch: arm64, ext: "" }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o dist/oplbm-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }} ./cmd/oplbm

  wails:
    # Wails desktop bundles per OS/arch; each archive ships the Wails-built
    # binary + THIRD_PARTY.md + licenses/ (BUILD-PLAN M4 6.4).
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux/amd64
            bundle: oplbm-linux-amd64.tar.gz
          - os: ubuntu-latest
            platform: linux/arm64
            bundle: oplbm-linux-arm64.tar.gz
          - os: windows-latest
            platform: windows/amd64
            bundle: oplbm-windows-amd64.zip
          - os: macos-latest
            platform: darwin/amd64
            bundle: oplbm-darwin-amd64.tar.gz
          - os: macos-latest
            platform: darwin/arm64
            bundle: oplbm-darwin-arm64.tar.gz
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install Linux Wails deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev build-essential pkg-config
      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
      - name: Build web
        run: ./scripts/build-web.sh
      - name: Third-party notices
        run: ./scripts/gen-third-party.sh
      - name: Wails build
        run: |
          ~/go/bin/wails build -platform ${{ matrix.platform }} -tags desktop -o oplbm
          ls -lh build/bin/
      - name: Bundle archive
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist bundle/stage
          cp -r build/bin/* bundle/stage/ 2>/dev/null || cp build/bin/oplbm* bundle/stage/ || true
          cp THIRD_PARTY.md bundle/stage/
          cp -r licenses bundle/stage/
          ls -R bundle/stage | head -n 80
          if [[ "${{ matrix.bundle }}" == *.zip ]]; then
            (cd bundle/stage && zip -r "$GITHUB_WORKSPACE/dist/${{ matrix.bundle }}" .)
          else
            tar -czf "dist/${{ matrix.bundle }}" -C bundle/stage .
          fi
          ls -lh dist/
      - name: Upload bundle
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.bundle }}
          path: dist/${{ matrix.bundle }}
          if-no-files-found: error
