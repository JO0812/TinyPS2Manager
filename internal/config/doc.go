// Package config holds user settings: theme (light/dark/system), default
// destination, BDM prefix, split threshold, staging temp directory, and
// filesystem-type default. Settings persist to SQLite alongside the job DB.
package config